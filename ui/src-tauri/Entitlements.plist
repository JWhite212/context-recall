<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for WebKit / WKWebView (Tauri's rendering engine) -->
    <key>com.apple.security.app-sandbox</key>
    <false/>

    <!-- Microphone access for audio recording -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Network access for localhost API communication and update checks -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- File access for reading/writing meeting data and config -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Hardened Runtime: WKWebView (Tauri's renderer) JITs JavaScript and
         maps unsigned executable memory; Hardened Runtime blocks both without
         these. disable-library-validation lets the app load the injected
         daemon + its unsigned-by-us dylibs. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
